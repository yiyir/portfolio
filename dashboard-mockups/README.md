# Dashboard Mockups

- [Compliance Dashboard mockup.pdf](./Compliance%20Dashboard%20mockup.pdf)

Click the link above to view the PDF — GitHub opens a built-in PDF preview for files in the repo. Note that GitHub strips `<embed>`/`<iframe>` tags from rendered Markdown, so the PDF can't be inlined directly into this README; linking to it is the standard way to surface a PDF from a repo page.
